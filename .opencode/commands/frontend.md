---
description: Implement or improve frontend/UI for the queued or specified story.
agent: frontend-developer
---

# /frontend

Implement or improve frontend/UI for the queued or specified story.

Instructions:
- Use the `frontend-developer` agent.
- Create or update the required file on disk.
- Do not return the artifact only in chat.
- Create parent directories if missing.
- After writing, verify the file exists and report the path.
- Keep scope controlled and route blockers to the appropriate blocker document.

User request:
{$ARGUMENTS}

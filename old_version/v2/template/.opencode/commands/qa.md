---
description: Run QA against acceptance criteria and write QA review; create bug report if failing.
agent: qa-engineer
---

# /qa

Run QA against acceptance criteria and write QA review; create bug report if failing.

Instructions:
- Use the `qa-engineer` agent.
- Create or update the required file on disk.
- Do not return the artifact only in chat.
- Create parent directories if missing.
- After writing, verify the file exists and report the path.
- Keep scope controlled and route blockers to the appropriate blocker document.

User request:
{$ARGUMENTS}
